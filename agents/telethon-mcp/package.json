{
  "name": "telethon-channel",
  "version": "1.0.0",
  "scripts": {
    "start": "bun install --no-summary && bun channel-server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "redis": "^4.6.0"
  }
}
