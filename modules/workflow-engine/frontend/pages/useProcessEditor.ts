/**
 * useProcessEditor — re-exported from canonical location.
 * Canonical: frontend/src/pages/useProcessEditor.ts
 *
 * This module uses @core alias (→ frontend/src) configured in vite.config.ts.
 * All logic lives in one place; deduplication per issue #444.
 */
export * from '@core/pages/useProcessEditor';
