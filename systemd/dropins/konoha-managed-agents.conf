[Service]
# Konoha starts managed agents in detached tmux sessions. On API restart, stop only
# the Bun bus process; agent sessions are supervised separately by agent-* services.
KillMode=process
TimeoutStopSec=15
