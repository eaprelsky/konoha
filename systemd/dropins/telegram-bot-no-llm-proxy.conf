[Service]
# Telegram Bot API is directly reachable from this host. The shared agent env
# carries LLM provider proxies that break Telegram TLS handshakes.
UnsetEnvironment=http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
